## Agent skills

### Issue tracker

GitHub Issues in this repo are tracked with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context repo: use the root `CONTEXT.md` and `docs/adr/` when available. See `docs/agents/domain.md`.
